{
  "private": true,
  "name": "@nyanbin/backend",
  "scripts": {
    "dev": "watchexec -r -e rs cargo run",
    "build": "cargo build --release",
    "check": "cargo check",
    "test": "cargo test",
    "test:server": "NYANBIN_MAX_ENVELOPE_BYTES=1048576 NYANBIN_LISTEN_ADDR=0.0.0.0:3000 cargo run",
    "test:prepare": "cargo build"
  }
}
